// lives CLASS
function Lives()
{
  // set default properties
  this.xpos = random(0, width);
  this.ypos = random(0,height);
  this.imageSize1 = random(0,70);
  this.imageSize2 = random(0,70);
  this.image = image
}

sprite1.prototype.display = function()
{
  imageMode(CENTER);

  // random hearts spawning
  image(sprite1, this.xpos, this.ypos, this.imageSize1, this.imageSize2);


    if (this.xpos > 500)
    {
      this.ypos ++5
    }else{
      draw(new image()++;

    }


}
